export type Contribution = {
    contribution_id: string
    workspace_id: string
    sender_id: string
    receiver_id: string
    points: number
    message: string
    reaction: number
    created_at: string
    update_at: string
};